const initialState = {
  title: `See what's happening in the world right now`,
  subTitle: 'Join twitter today'
}

const signupBlock = (state = initialState, action) => {
  switch(action.type) {
    default:
      return state;
  }
}

export default signupBlock;