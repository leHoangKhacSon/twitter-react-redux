const inititalState = {
  homebarName: 'Home',
  homebarIcon: 'home-bar-icon fab fa-jedi-order'
};

const homeBar = (state = inititalState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default homeBar;